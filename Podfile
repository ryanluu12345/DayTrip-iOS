# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DayTrip' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DayTrip
  pod 'AlamofireImage'
  pod 'Firebase/Core' 
  pod 'Firebase/Auth'
  pod 'GoogleSignIn'

  target 'DayTripTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DayTripUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
